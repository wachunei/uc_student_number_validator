require 'spec_helper'

describe UcStudentNumberValidator do
  it 'has a version number' do
    expect(UcStudentNumberValidator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
